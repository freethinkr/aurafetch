# AuraFetch Configuration
# ~/.config/aurafetch/aurafetch.conf

# Show public IP (requires internet, can be slow)
SHOW_PUBLIC_IP=false

# Logo size: normal | small
LOGO_SIZE=normal

# Override distro logo (leave empty for auto-detect)
# LOGO_OVERRIDE=arch

# Compact mode (fewer details)
COMPACT=false
